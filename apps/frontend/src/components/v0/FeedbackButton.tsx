import React from "react";
import { Button } from "@/components/ui/button";

const FeedbackButton = () => {
  return (
    <Button className="bg-black text-[9px] text-white rounded-[3px] hover:bg-gray-800 p-2 h-5">
      FEEDBACK
    </Button>
  );
};

export default FeedbackButton;
