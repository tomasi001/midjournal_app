import React from "react";
import { Button } from "@/components/ui/button";

const FeedbackButton = () => {
  return (
    <Button className="bg-black text-white border border-white rounded-full hover:bg-gray-800">
      FEEDBACK
    </Button>
  );
};

export default FeedbackButton;
