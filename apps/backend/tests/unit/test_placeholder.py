def test_always_passes():
    """
    A placeholder test that always passes.
    This ensures that the test suite runs correctly in CI environments
    where no other tests have been defined yet.
    """
    assert True
